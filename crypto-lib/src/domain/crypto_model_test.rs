#[cfg(test)]
mod crypto_model_test {
    // use crate::domain::crypto_config::CryptoConfig;
    #[test]
    fn crypto_model_test() {
        // let crypto_config = CryptoConfig::new(10, 10);
        // println!("{:?}", crypto_config);
    }
}